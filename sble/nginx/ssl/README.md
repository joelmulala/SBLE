# TLS certificates (not committed)

Place institutional or Let's Encrypt files here for production Nginx, for example:

- `fullchain.pem`
- `privkey.pem`

Configure `listen 443 ssl` in a production server block (see `DEPLOYMENT.md`).
